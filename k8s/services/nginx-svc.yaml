---
apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: dev
  labels:
    app: nginx-deploy
spec:
  selector:
    app: nginx-deploy
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
